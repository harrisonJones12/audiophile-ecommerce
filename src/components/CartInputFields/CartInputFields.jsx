import React from 'react'
import PropTypes from 'prop-types'

function CartInputFields(inputFieldArr) {


    // const renderInputFieldsSection = (
    //     <>{
    //         inputFieldArr.map(() => {

    //         })}
    //     </>
    // );


    return (
        <>

        </>
    )
}

CartInputFields.propTypes = {
    inputFieldArr: PropTypes.array
}

export default CartInputFields
